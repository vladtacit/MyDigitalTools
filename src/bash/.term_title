# Insert this into ~/.bashrc
#if [ -f ~/Data/System/bash/.term_title ]; then
#    . ~/Data/System/bash/.term_title
#fi

function stt() {
	if [[ -z "$ORIG" ]]; then
		ORIG=$PS1
	fi

	TITLE="\[\e]2;$*\a\]"
	PS1=${ORIG}${TITLE}
}

